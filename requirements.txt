Flask>=2.3.0           # Web 框架
lxml>=4.9.0            # 处理XML（docx底层）
pandas>=1.5.0          # 处理Excel
python-docx>=0.8.11    # 处理docx文档
requests>=2.28.0       # HTTP请求（AI API）
numpy>=1.21.0          # 数组运算（辅助）
docx2txt>=0.8          # 旧版doc文本提取
beautifulsoup4>=4.11.0 # HTML解析（mammoth辅助）
mammoth>=1.6.0         # doc转docx备用方案
werkzeug>=2.3.0        # Flask依赖
openpyxl>=3.0.0        # 读写Excel（pandas依赖） 